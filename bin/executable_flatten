#!/bin/bash

SEARCH=${1}
for FILE in `find -name ${SEARCH}.xmlx -or -name ${SEARCH}.pdf`; do
  EXT="${FILE##*.}" 
  DIR=$(dirname "$FILE")
  NEWFILE=$(basename "$DIR")
  cp "$FILE" "$NEWFILE.${EXT}"
done
